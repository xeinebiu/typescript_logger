/**
 * Represents the type of an output
 * @author xeinebiu
 */
export enum OutputType {
    debug = 'debug',
    info = 'info',
    log = 'log',
    error = 'error',
    warn = 'warn',
    trace = 'trace'
}
